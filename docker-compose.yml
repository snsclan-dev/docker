version: "3"

services:
    mysql:
        container_name: mysql
        image: mysql:8.0
        restart: always
        volumes:
            - ${DATA_PATH}/mysql:/var/lib/mysql
            - ./mysql/my.cnf:/etc/my.cnf
            - ./mysql/init.sql:/docker-entrypoint-initdb.d/init.sql
        environment:
            MYSQL_ROOT_PASSWORD: ${PASSWORD}
            TZ: Asia/Seoul
        ports:
            - ${MYSQL_PORT}:3306
    mongo:
        container_name: mongo
        image: mongo
        restart: always
        environment:
            MONGO_INITDB_ROOT_USERNAME: root
            MONGO_INITDB_ROOT_PASSWORD: ${PASSWORD}
        ports:
            - ${MONGO_PORT}:27017
        volumes:
            - ${DATA_PATH}/mongo:/data
    redis:
        container_name: redis
        image: redis
        restart: always
        volumes:
            - ${DATA_FOLDER}/redis:/data
            - ./redis/redis.conf:/usr/local/etc/redis/redis.conf
        ports:
            - ${REDIS_PORT}:6379
        command: 
            redis-server /usr/local/etc/redis/redis.conf
    php:
        container_name: php
        image: php:8.2-fpm
        restart: always
        ports:
            - "9000:9000"
        volumes:
            - ./php/index.php:/var/www/html
    npmplus:
        container_name: npmplus
        image: zoeyvid/npmplus
        restart: always
        network_mode: host
        ports:
            - "80:80"
        volumes:
            - ${DATA_FOLDER}/npmplus:/data
            # - ./nginx_php.conf:/etc/nginx/sites-available/nginx_php.conf # NGINX 구성 파일 마운트
            - ./npm/rhymix.conf:/etc/nginx/sites-available/rhymix.conf
            # - ./sites-enabled:/etc/nginx/sites-enabled # 심볼릭 링크 디렉토리 마운트
            # - "/var/www:/var/www" # optional, if you want to use it as webserver for html/php
            # - /var/www/rhymix:/var/www/rhymix" # optional, if you want to use it as webserver for html/php
            # - ./npm/http.conf:/data/nginx/custom/http.conf
            - ./npm/http_top.conf:/data/nginx/custom/http_top.conf
            - ./npm/events.conf:/data/nginx/custom/events.conf
            - ./npm/server_proxy.conf:/data/nginx/custom/server_proxy.conf
        environment:
            - "TZ=Asia/Seoul"
            - "LOGROTATE=true" # Enables writing http access logs to /opt/npm/nginx/access.log, stream access logs to /opt/npm/nginx/stream.log and enables daily logrotation, default false
            - "LOGROTATIONS=7" # Set how often the access.log should be rotated until it is deleted, default 3
            - "PHP_HOST=php"
            - "PHP_PORT=9000" # PHP 서비스의 포트
        depends_on:
            - php
    wordpress:
        container_name: wordpress
        image: wordpress
        restart: always
        ports:
            - ${WORDPRESS_PORT}:80
        environment:
            WORDPRESS_DB_HOST: ${SERVER}:${MYSQL_PORT}
            WORDPRESS_DB_USER: root
            WORDPRESS_DB_PASSWORD: ${PASSWORD}
            WORDPRESS_DB_NAME: wordpress
        volumes:
            - ${DATA_PATH}/wordpress:/var/www/html
        depends_on:
            - mysql
    portainer:
        container_name: portainer
        image: 'portainer/portainer-ce:latest'
        restart: always
        ports:
            - '8000:8000'
            - ${PORTAINER_PORT}:9000
        volumes:
            - ${DATA_FOLDER}/portainer:/data
            - /var/run/docker.sock:/var/run/docker.sock
    netdata:
        container_name: netdata
        image: netdata/netdata
        pid: host
        network_mode: host
        restart: unless-stopped
        cap_add:
            - SYS_PTRACE
            - SYS_ADMIN
        security_opt:
            - apparmor:unconfined
        volumes:
            - ${DATA_FOLDER}/netdata:/etc/netdata
            - ${DATA_FOLDER}/netdatalib:/var/lib/netdata
            - ${DATA_FOLDER}/netdatacache:/var/cache/netdata
            - /etc/passwd:/host/etc/passwd:ro
            - /etc/group:/host/etc/group:ro
            - /etc/localtime:/etc/localtime:ro
            - /proc:/host/proc:ro
            - /sys:/host/sys:ro
            - /etc/os-release:/host/etc/os-release:ro
            - /var/log:/host/var/log:ro
            - /var/run/docker.sock:/var/run/docker.sock:ro